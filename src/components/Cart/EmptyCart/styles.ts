import { styled } from "../../../styles";

export const EmptyCartContainer = styled('div', {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',

    span: {
        fontSize: '1rem',
        textAlign: 'center'
    }
})